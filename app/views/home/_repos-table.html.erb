<div>
  <table>
    <thead>
      <tr>
        <th>Name</th>
      </tr>
    </thead>
    <tbody>
    <% repositories.each do |repository| %>
      <tr>
        <td><a href="<%= repository['html_url'] %>" target='_blank'><%= repository['name'] %></a> </td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
<%if repositories.length > 0 %>
  <%= link_to "&laquo; Previous".html_safe, github_repositories_path(page: page - 1, query: query), class: "previous", remote: true %>
  <%= link_to "&raquo; Next".html_safe, github_repositories_path(page: page + 1, query: query), class: "next", remote: true %>
<% end %>